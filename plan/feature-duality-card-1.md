---
goal: "Implementare il biglietto da visita 3D duale 'The Engineer / The Human' con mondo reattivo notte-alba, interazioni multi-tier, audio opt-in e progressive enhancement"
version: 1.1
date_created: 2026-04-14
last_updated: 2026-04-15
owner: "Ishar Pasquale Mannara Alam"
status: 'Completed'
tags: [feature, design-system, interaction, animation, 3d, performance, accessibility, audio]
---

# Introduction

![Status: Completed](https://img.shields.io/badge/status-Completed-brightgreen)

Piano di implementazione deterministico ed eseguibile per costruire l'esperienza "DUALITY CARD" definita nel documento `duality-card-design-plan.md`. Il progetto trasforma l'attuale sito `index.html` in un biglietto da visita 3D interattivo con due facce â€” "The Engineer" (notte) e "The Human" (alba) â€” fluttuante in un mondo generativo che si trasforma al flip. Stack: HTML/CSS/JS vanilla, architettura modulare ES Modules, performance target 60fps, accessibilitÃ  WCAG AA, audio opt-in con synthesis Web Audio/Tone.js.

## 1. Requirements & Constraints

- **REQ-001**: Card 3D con due facce (`card-front` / `card-back`), flip bidirezionale sincronizzato allo sfondo. Durata flip: `2.0s`, easing: `cubic-bezier(0.4, 0, 0.2, 1)`. Transizione sfondo inizia al 50% del flip.
- **REQ-002**: Ambiente "Living World" a 5 layer: gradient animato (L0), particelle generative (L1), vignetta radiale (L2), noise grain canvas (L3), card top (L4).
- **REQ-003**: Stato NOTTE (fronte attivo): sfondo `#02020a`, gradient radiale `#0a0a1a â†’ #02020a`, 180 stelle (0.5â€“2px, drift 0.015px/frame), parallasse 8%/2%.
- **REQ-004**: Stato ALBA (retro attivo): sfondo `#f5ede0`, gradient `#f0dfc0 â†’ #faf6f0`, 60 pollini ovali (drift 0.03px/frame), parallasse 5%.
- **REQ-005**: Card dimensioni desktop 420Ã—265px, aspect ratio 1.585:1, min width 300px. Float sinusoidale (amp 10px, period 3.2s). Tilt mouse Â±18deg X / Â±22deg Y con lerp 0.08.
- **REQ-006**: Fronte "The Engineer": materiale carbonio woven (SVG tile 8Ã—8, 45deg), palette notte tech (#0d0d14, #e8eaf0, #2979ff, #00e5ff), font JetBrains Mono (300/400/700), micro-animazioni (typing 60ms/char, LED blink async 2.1s/3.7s, trace SVG 1.2s, logo rotateY 8s, glitch scan 400ms ogni ~20s).
- **REQ-007**: Retro "The Human": materiale tela lino (SVG turbulence + displacement), palette alba (#faf6f0, #2c2416, #c4622d, #5a7a52, dettagli #8b7355, texture #f0e8d8), font Playfair Display (700 italic, 400 italic) + Lora (400, 600, 700), layout bio+timeline+hobby, micro-animazioni (stagger fade 150ms, timeline hover tooltip, hobby icon themed motion, macchia caffÃ¨, francobollo TORINO). Tipografia retro include anche "Lora weight 600 small caps" per label/categorie e "Playfair Display italic grande centrata" per citazione.
- **REQ-008**: Interazioni I1â€“I16 organizzate in 4 tier di discovery. Flip tramite doppio click bordo lungo (I7). Drag con spring physics (I9). Easter eggs: Konami code pixel-art 3s (I13), triple click confetti (I14), shake glitch (I15), long press piega angoli (I16).
- **REQ-009**: Audio opt-in: toast al primo click `"ðŸ”Š Enable sounds?"`, persistenza `localStorage`, master volume 0.35. 12 eventi sonori (S1â€“S12) generati via Web Audio API synthesis con fallback Tone.js lazy-loaded.
- **REQ-010**: Responsive 3 breakpoint: desktop >900px (full), tablet 600â€“900px (tilt Â±10deg, card ridotta), mobile <600px (swipe flip, gyroscope, no drag, particelle dimezzate, hint landscape).
- **REQ-011**: ContinuitÃ  visiva fronte/retro: durante il flip il bordo mostra la transizione tra pattern carbonio e tela lino. Il logo fronte appare come "sigillo ink stamp" sul retro. Accento blu (fronte) e terracotta (retro) sono complementari su ruota cromatica.
- **REQ-012**: Bordo laterale retro (thickness): gradient `#e8d5b8 â†’ #d4b896`, fregio ornamentale ripetuto ispirazione carta da lettere italiana sul bordo lungo destro, visibile solo con angolazione >10deg.
- **REQ-013**: Transizione sonora al flip: Notteâ†’Alba fade-out toni digitali (80ms), crossfade suono flip (S2+S3), fade-in silenzio caldo organico, durata totale 2.2s. Albaâ†’Notte specularmente opposto.
- **SEC-001**: Nessuno script remoto non essenziale. Se CDN utilizzati (Google Fonts, Tone.js), applicare `integrity` + `crossorigin="anonymous"`. Nessun uso di `eval()` o costruzione dinamica codice.
- **SEC-002**: Sanitizzare tutti gli input da eventi tastiera/pointer/touch. Rate-limiting su easter egg trigger per prevenire abuse.
- **A11-001**: Rispettare `prefers-reduced-motion: reduce` â€” disabilitare float, tilt, particelle animate, grain, typing, glitch. Mantenere solo transizioni istantanee e contenuto statico leggibile.
- **A11-002**: Rispettare `prefers-reduced-transparency` â€” rimuovere overlay semi-trasparenti e grain.
- **A11-003**: Focus visibile su tutti gli elementi interattivi. Navigazione tastiera per flip (`Enter`/`Space` su bordo focusable), toggle audio (`A`), dismiss hint (`Escape`). Contrasto testo â‰¥ 4.5:1 WCAG AA.
- **PER-001**: JS bundle < 80KB gzipped (escluso Tone.js opzionale). CSS totale < 15KB. Target 60fps su hardware mid-range (2020+).
- **PER-002**: Animazioni CSS su proprietÃ  composited (`transform`, `opacity`). Canvas offscreen per grain dove supportato (`OffscreenCanvas`).
- **PER-003**: Monitor fps rolling: se media < 30fps per 2s consecutivi â†’ dimezzare particelle attive, disabilitare grain animation, ridurre shadow blur.
- **CON-001**: Stack obbligatorio: HTML/CSS/JS vanilla, zero framework UI, zero bundler obbligatorio. Sito statico servito da GitHub Pages.
- **CON-002**: Asset QR e logo in SVG inline o file `.svg` locali nella cartella `assets/`. Nessun raster per elementi vettoriali.
- **CON-003**: CompatibilitÃ  browser: Chrome 90+, Firefox 90+, Safari 15+, Edge 90+ (desktop). Chrome Android 90+, Safari iOS 15+ (mobile).
- **CON-004**: Nessun file audio precaricato al page load. Tutti i suoni sintetizzati runtime dopo consenso utente.
- **GUD-001**: Struttura file conforme a sezione "9. FILE STRUCTURE" del design plan. Directory: `style/`, `js/`, `assets/`.
- **GUD-002**: Ogni modulo JS esporta una singola funzione `init*()` chiamata da `main.js`. Comunicazione inter-modulo via `CustomEvent` sul `document`.
- **PAT-001**: Singolo loop `requestAnimationFrame` in `js/card.js` con delta-time (`performance.now()`), dispatch valori a scene.js per sincronizzazione particelle.
- **PAT-002**: State machine in `js/scene.js` con stati `'night'` | `'dawn'` e transizioni temporizzate 2000ms trigger da evento `flip:midpoint`.
- **PAT-003**: Debounce input pointer (16ms), throttle scroll (100ms), debounce resize (250ms).

## 2. Implementation Steps

### Implementation Phase 1 â€” Project Scaffolding & Design Tokens

- GOAL-001: Creare la struttura file completa del progetto, definire tutte le CSS custom properties (design tokens) e preparare il markup semantico HTML della scena 3D con contenuti fronte/retro.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-001 | Creare le directory `style/`, `js/`, `assets/` nella root del progetto. Creare i file vuoti: `style/base.css`, `style/scene.css`, `style/card.css`, `style/front.css`, `style/back.css`, `style/interactions.css`, `js/main.js`, `js/scene.js`, `js/card.js`, `js/interactions.js`, `js/audio.js`. Creare file `.nojekyll` nella root (file vuoto, necessario per GitHub Pages con directory stile). | ✅ | 2026-04-15 |
| TASK-002 | In `style/base.css`: definire CSS reset (`*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }`). Definire `:root` con tutte le custom properties: **Palette Notte** (`--night-bg: #02020a`, `--night-gradient-from: #0a0a1a`, `--night-gradient-to: #02020a`, `--night-tint: #1a3aff`, `--night-card-bg: #0d0d14`, `--night-text: #e8eaf0`, `--night-accent: #2979ff`, `--night-accent2: #00e5ff`, `--night-meta: #4a5568`, `--night-weave1: #141420`, `--night-weave2: #0f0f1a`). **Palette Alba** (`--dawn-bg: #f5ede0`, `--dawn-gradient-from: #f0dfc0`, `--dawn-gradient-to: #faf6f0`, `--dawn-tint: #d4862a`, `--dawn-card-bg: #faf6f0`, `--dawn-text: #2c2416`, `--dawn-accent: #c4622d`, `--dawn-accent2: #5a7a52`, `--dawn-detail: #8b7355`, `--dawn-texture: #f0e8d8`). **Timing** (`--flip-duration: 2.0s`, `--flip-easing: cubic-bezier(0.4, 0, 0.2, 1)`, `--float-amplitude: 10px`, `--float-period: 3.2s`, `--tilt-lerp: 0.08`, `--tilt-max-x: 18deg`, `--tilt-max-y: 22deg`). **Z-index** (`--z-gradient: 0`, `--z-particles: 1`, `--z-vignette: 2`, `--z-grain: 3`, `--z-card: 4`, `--z-ui: 10`). **Breakpoints comment** (`/* desktop: >900px, tablet: 600-900px, mobile: <600px */`). **Border radius** (`--radius-front: 6px`, `--radius-back: 8px`). **Card size** (`--card-width: 420px`, `--card-height: 265px`, `--card-min-width: 300px`). Applicare `html { font-size: 16px; scroll-behavior: smooth; }` e `body { overflow: hidden; margin: 0; height: 100vh; -webkit-font-smoothing: antialiased; }`. | ✅ | 2026-04-15 |
| TASK-003 | In `style/base.css`: aggiungere `@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&family=Playfair+Display:ital,wght@0,700;1,400;1,700&family=Lora:wght@400;600;700&display=swap')`. Definire variabili font: `--font-mono: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace`, `--font-display: 'Playfair Display', 'Georgia', serif`, `--font-body: 'Lora', 'Cambria', serif`. | ✅ | 2026-04-15 |
| TASK-004 | Riscrivere completamente `index.html` sostituendo il contenuto attuale. Struttura HTML: `<!DOCTYPE html><html lang="en"><head>` con meta charset, viewport, description, canonical, favicon, OG tags (preservare valori esistenti), link ai 6 CSS (`style/base.css`, `style/scene.css`, `style/card.css`, `style/front.css`, `style/back.css`, `style/interactions.css`). `<body>` contiene: `<div class="scene" role="main">` â†’ `<div class="world-layer world-gradient"></div>` + `<canvas class="world-layer world-particles" aria-hidden="true"></canvas>` + `<div class="world-layer world-vignette" aria-hidden="true"></div>` + `<canvas class="world-layer world-grain" aria-hidden="true"></canvas>` + `<div class="card-wrapper">` â†’ `<div class="card" tabindex="0" aria-label="Interactive business card - double click edge to flip">` â†’ `<div class="card-face card-front">` (contenuto fronte, vedi TASK-005) + `<div class="card-face card-back">` (contenuto retro, vedi TASK-006) + `</div></div></div>`. Dopo `.scene`: `<div class="audio-toast" role="dialog" aria-label="Sound preference" hidden>` con testo e bottoni Yes/No. Prima di `</body>`: `<script type="module" src="js/main.js"></script>`. | ✅ | 2026-04-15 |
| TASK-005 | Dentro `.card-front` in `index.html`: aggiungere markup fronte. Struttura: `<div class="front-corner-dots" aria-hidden="true"><span></span><span></span><span></span></div>` + `<div class="front-leds" aria-hidden="true"><button class="led led-1" aria-label="LED toggle"></button><button class="led led-2" aria-label="LED toggle"></button></div>` + `<svg class="front-traces" aria-hidden="true">` (4 path angolari, stroke `var(--night-accent2)`, stroke-width 1, fill none) `</svg>` + `<h1 class="front-name" data-text="Ishar Pasquale Mannara Alam"></h1>` + `<p class="front-role">automation architect & r&d engineer</p>` + `<div class="front-metadata"><span>created: 1997</span><span>language: py, c++, js</span><span>status: building</span><span>uptime: 7y+</span></div>` + `<div class="front-bottom"><div class="front-qr" aria-label="QR code to GitHub profile"><!-- SVG QR inline --></div><div class="front-logo" aria-hidden="true"><!-- logo SVG --></div></div>` + `<div class="front-edge-text" aria-hidden="true">// IMAN.PASQUALE // v2.0 //</div>`. | ✅ | 2026-04-15 |
| TASK-006 | Dentro `.card-back` in `index.html`: aggiungere markup retro. Struttura: `<div class="back-stamp" aria-hidden="true">TORINO</div>` + `<div class="back-header"><h2 class="back-name">Ishar Pasquale Mannara Alam</h2><div class="back-avatar" aria-hidden="true"><!-- avatar/sigillo SVG --></div></div>` + `<div class="back-bio"><h3>Chi sono</h3><p>Ingegnere di giorno, curioso di notte.<br>Costruisco macchine che pensano.</p></div>` + `<ol class="back-timeline"><li data-year="1997" data-detail="Nato a ..."><span class="tl-dot"></span><span class="tl-year">1997</span><span class="tl-text">Nato a ___</span></li><li data-year="2015" data-detail="Prima riga di codice"><span class="tl-dot"></span><span class="tl-year">2015</span><span class="tl-text">Prima riga di codice</span></li><li data-year="2019" data-detail="Primo robot autonomo"><span class="tl-dot"></span><span class="tl-year">2019</span><span class="tl-text">Primo robot autonomo</span></li><li data-year="2023" data-detail="1.5Mâ‚¬ generati"><span class="tl-dot"></span><span class="tl-year">2023</span><span class="tl-text">1.5Mâ‚¬ generati</span></li></ol>` + `<div class="back-hobbies"><button class="hobby" data-hobby="music" aria-label="Music">â™ª</button><button class="hobby" data-hobby="travel" aria-label="Travel">âœˆ</button><button class="hobby" data-hobby="sport" aria-label="Sport">âš½</button><button class="hobby" data-hobby="photo" aria-label="Photography">ðŸ“·</button><button class="hobby" data-hobby="nature" aria-label="Nature">ðŸŒ±</button></div>` + `<div class="back-bottom"><div class="back-qr" aria-label="QR code to personal message"><!-- QR SVG --></div><span class="back-location">Torino, IT</span></div>` + `<div class="back-coffee" aria-hidden="true" role="button" aria-label="Coffee stain easter egg"></div>`. | ✅ | 2026-04-15 |
| TASK-007 | In `js/main.js`: definire modulo ES con imports: `import { initScene } from './scene.js'`, `import { initCard } from './card.js'`, `import { initInteractions } from './interactions.js'`, `import { initAudio } from './audio.js'`. Funzione `boot()` che chiama in sequenza: `initScene()`, `initCard()`, `initInteractions()`, `initAudio()`. Listener `document.addEventListener('DOMContentLoaded', boot)`. Esportare nulla (entry point). | ✅ | 2026-04-15 |
| TASK-008 | In ciascun modulo JS (`js/scene.js`, `js/card.js`, `js/interactions.js`, `js/audio.js`): creare funzione esportata `export function init*() {}` (corpo vuoto, placeholder). Aggiungere commento header con responsabilitÃ  del modulo. | ✅ | 2026-04-15 |

### Implementation Phase 2 â€” Living World Engine (Gradient, Particles, Grain, Transitions)

- GOAL-002: Implementare il motore scena dinamica completo: gradient notte/alba, sistema particelle generative (stelle e pollini), grain noise canvas, vignetta, parallasse cursore, transizione sincronizzata al flip e monitor performance adattivo.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-009 | In `style/scene.css`: stilare `.scene` con `position: relative; width: 100vw; height: 100vh; overflow: hidden;`. Stilare `.world-layer` con `position: absolute; inset: 0;`. Stilare `.world-gradient` con `z-index: var(--z-gradient); transition: background var(--flip-duration) var(--flip-easing);`. Stato default (notte): `background: radial-gradient(ellipse at 50% 35%, var(--night-gradient-from), var(--night-gradient-to));`. Stato `.scene.is-dawn .world-gradient`: `background: linear-gradient(to top, var(--dawn-gradient-from), var(--dawn-gradient-to));`. Stilare `.world-particles` con `z-index: var(--z-particles);`. Stilare `.world-vignette` con `z-index: var(--z-vignette); background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.4) 100%); pointer-events: none;`. Stilare `.world-grain` con `z-index: var(--z-grain); pointer-events: none; opacity: 0.03;`. Aggiungere `.scene.is-dawn .world-grain { opacity: 0.02; }`. Aggiungere `.scene.is-dawn .world-vignette { background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.15) 100%); }`. | ✅ | 2026-04-15 |
| TASK-010 | In `js/scene.js` funzione `initScene()`: implementare state machine con variabile `let worldState = 'night'`. Listener su `document.addEventListener('flip:midpoint', () => { ... })` che toglie/aggiunge classe `is-dawn` su `document.querySelector('.scene')`, aggiorna `worldState`, e chiama `transitionParticles()`. Funzione `getWorldState()` esportata per uso da altri moduli. | ✅ | 2026-04-15 |
| TASK-011 | In `js/scene.js`: implementare sistema particelle su canvas `.world-particles`. Classe `Particle` con proprietÃ : `x, y, size, opacity, vx, vy, type ('star'|'pollen'), fadingOut, fadingIn`. Funzione `createStars(count=180)`: particelle con size random 0.5â€“2px, drift 0.015, posizione random, colore bianco con opacity random 0.3â€“1.0. Funzione `createPollen(count=60)`: particelle ovali (drawEllipse), drift 0.03, leggera gravitÃ  verso il basso (+0.005 vy), colore `rgba(212,134,42,0.4)`. Inizializzare con `createStars(180)`. | ✅ | 2026-04-15 |
| TASK-012 | In `js/scene.js`: implementare render loop particelle dentro singolo rAF condiviso (chiamato da `card.js` via export `updateScene(deltaTime)`). Per ogni particella: aggiornare posizione con drift + browniano. Wrap-around ai bordi canvas. Disegnare: stelle come `arc` cerchio, pollini come ellissi (`ellipse()`). Implementare parallasse: calcolare offset da posizione cursore (event `mousemove` su `document`). Stelle vicine (size >1.2px): shift 8% cursore. Stelle lontane: shift 2%. Pollini: shift 5%. | ✅ | 2026-04-15 |
| TASK-013 | In `js/scene.js`: implementare `transitionParticles()`. Al trigger: impostare `fadingOut = true` su tutte le particelle attive, opacity decresce a 0 in 800ms. Al raggiungimento di opacity 0: rimuovere particella dall'array. Contemporaneamente creare nuove particelle del tipo opposto con `fadingIn = true`, opacity cresce da 0 a target in 800ms. | ✅ | 2026-04-15 |
| TASK-014 | In `js/scene.js`: implementare grain noise su canvas `.world-grain`. Funzione `renderGrain(ctx, width, height)`: creare `ImageData`, riempire pixel con valori random grayscale (0â€“255) a canale alpha fisso (8 per notte / 5 per alba). Chiamare `renderGrain()` ogni 80ms via `setInterval`. Se `OffscreenCanvas` Ã¨ supportato (`typeof OffscreenCanvas !== 'undefined'`), usare `transferControlToOffscreen()` e rendere grain in worker inline (blob URL). Altrimenti rendere su canvas principale. | ✅ | 2026-04-15 |
| TASK-015 | In `js/scene.js`: implementare tint/aureola. In stato notte: aggiungere radial-gradient centrato sulla card con colore `rgba(26,58,255,0.04)` via CSS class `.scene.is-night .world-gradient::after`. In stato alba: `rgba(212,134,42,0.06)`. Posizione calcolata dalla posizione corrente della card. Implementare come pseudo-elemento posizionato con `pointer-events: none`. | ✅ | 2026-04-15 |
| TASK-016 | In `js/scene.js`: implementare monitor fps. Variabile `let fpsHistory = []`. Ad ogni frame rAF: calcolare fps da deltaTime, push in array, mantenere ultimi 120 campioni (2s a 60fps). Funzione `checkPerformance()` chiamata ogni 2000ms: se media `fpsHistory` < 30 â†’ settare flag `lowPerformance = true`, dimezzare array particelle attive (splice ogni 2Â°), settare `grainInterval` a 160ms. Se media risale > 45 per 2s: ripristinare. Export `isLowPerformance()`. | ✅ | 2026-04-15 |

### Implementation Phase 3 â€” Card 3D Physics (Float, Tilt, Flip, Shadow, Specular)

- GOAL-003: Implementare la fisica completa della card 3D: galleggiamento continuo sinusoidale, tilt reattivo al cursore con smoothing, flip bidirezionale con eventi lifecycle, ombra dinamica, highlight speculare e thickness simulata.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-017 | In `style/card.css`: stilare `.card-wrapper` con `position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; z-index: var(--z-card); perspective: 900px; perspective-origin: 50% 45%;`. Stilare `.card` con `width: var(--card-width); height: var(--card-height); min-width: var(--card-min-width); transform-style: preserve-3d; position: relative; cursor: grab; will-change: transform;`. Stilare `.card-face` con `position: absolute; inset: 0; backface-visibility: hidden; -webkit-backface-visibility: hidden;`. Stilare `.card-front` con `border-radius: var(--radius-front);`. Stilare `.card-back` con `border-radius: var(--radius-back); transform: rotateY(180deg);`. | ✅ | 2026-04-15 |
| TASK-018 | In `style/card.css`: aggiungere pseudo-elemento `.card::after` per highlight speculare: `content: ''; position: absolute; inset: 0; border-radius: inherit; background: radial-gradient(circle at 50% 50%, rgba(255,255,255,0.15), transparent 60%); opacity: 0; pointer-events: none; transition: opacity 0.3s; z-index: 5;`. La posizione e opacity sono controllate da JS via CSS custom properties `--spec-x`, `--spec-y`, `--spec-opacity`. | ✅ | 2026-04-15 |
| TASK-019 | In `js/card.js` funzione `initCard()`: cachare riferimenti DOM: `scene = document.querySelector('.scene')`, `cardWrapper = document.querySelector('.card-wrapper')`, `card = document.querySelector('.card')`. Inizializzare stato: `let currentRotX = 0, currentRotY = 0, currentRotZ = 0, floatY = 0, targetTiltX = 0, targetTiltY = 0, isFlipping = false, isFlipped = false, isDragging = false`. Avviare loop rAF principale `function animate(now)` con calcolo `deltaTime = (now - lastTime) / 1000; lastTime = now;`. Nel loop: chiamare `updateFloat(now)`, `updateTilt()`, `applyTransform()`, `updateScene(deltaTime)` (importata da scene.js), poi `requestAnimationFrame(animate)`. | ✅ | 2026-04-15 |
| TASK-020 | In `js/card.js`: implementare `updateFloat(now)`. Calcolare `const t = now / 1000;`. `floatY = Math.sin(t * (2 * Math.PI / 3.2)) * 10;` (period 3.2s, amplitude 10px). Rotazioni idle: `idleRotX = Math.sin(t * 0.7) * 2.5;`, `idleRotY = Math.sin(t * 0.5) * 1.8;`, `idleRotZ = Math.sin(t * 0.3) * 0.4;`. | ✅ | 2026-04-15 |
| TASK-021 | In `js/card.js`: implementare tilt mouse. Listener `document.addEventListener('mousemove', onMouseMove)`. In `onMouseMove(e)`: calcolare posizione normalizzata [-1, 1] relativa al centro viewport. `targetTiltX = clamp(normalizedY * -18, -18, 18);` `targetTiltY = clamp(normalizedX * 22, -22, 22);`. In `updateTilt()`: `currentRotX = lerp(currentRotX, targetTiltX + idleRotX, 0.08);` `currentRotY = lerp(currentRotY, targetTiltY + idleRotY, 0.08);` `currentRotZ = lerp(currentRotZ, idleRotZ, 0.08);`. Funzione helper `lerp(a, b, t) { return a + (b - a) * t; }`, `clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }`. | ✅ | 2026-04-15 |
| TASK-022 | In `js/card.js`: implementare `applyTransform()`. Costruire stringa transform: `card.style.transform = \`translateY(\${floatY}px) rotateX(\${currentRotX}deg) rotateY(\${currentRotY + (isFlipped ? 180 : 0)}deg) rotateZ(\${currentRotZ}deg)\`;`. Aggiornare specular highlight: `card.style.setProperty('--spec-x', \`\${50 + currentRotY * 1.5}%\`);` `card.style.setProperty('--spec-y', \`\${50 - currentRotX * 1.5}%\`);` `card.style.setProperty('--spec-opacity', \`\${Math.min(0.15, (Math.abs(currentRotX) + Math.abs(currentRotY)) * 0.003)}\`);`. | ✅ | 2026-04-15 |
| TASK-023 | In `js/card.js`: implementare ombra dinamica. In `applyTransform()`: calcolare `shadowX = -currentRotY * 1.2;` `shadowY = 20 + currentRotX * 0.8;` `shadowBlur = 40 + Math.abs(floatY) * 4;`. Determinare colore ombra dal world state (import `getWorldState` da scene.js). **Notte (fronte)**: `0 0 0 1px rgba(0,140,255,0.15), 0 20px 60px rgba(0,0,0,0.7), 0 0 80px rgba(41,121,255,0.12)`. **Alba (retro)**: `0 0 0 1px rgba(196,98,45,0.2), 0 20px 50px rgba(120,80,30,0.25), 0 0 60px rgba(212,134,42,0.1)`. Le ombre cambiano dinamicamente col float â€” intensitÃ  aumenta quando card Ã¨ "in alto" nel ciclo. | ✅ | 2026-04-15 |
| TASK-024 | In `js/card.js`: implementare flip. Funzione `flipCard()`: se `isFlipping` return. Settare `isFlipping = true`. Dispatch `document.dispatchEvent(new CustomEvent('flip:start', { detail: { from: isFlipped ? 'dawn' : 'night' }}))`. Usare `card.style.transition = 'transform var(--flip-duration) var(--flip-easing)'`. Timeout a 1000ms (50% di 2.0s): dispatch `document.dispatchEvent(new CustomEvent('flip:midpoint'))`. Timeout a 2000ms: `isFlipping = false; isFlipped = !isFlipped;` dispatch `document.dispatchEvent(new CustomEvent('flip:end', { detail: { face: isFlipped ? 'back' : 'front' }}))`, rimuovere `card.style.transition`. Esportare `flipCard` e `isCardFlipped()`. | ✅ | 2026-04-15 |
| TASK-025 | In `style/card.css`: implementare thickness simulata. Pseudo-elemento `.card::before` per bordo laterale sinistro fronte: `content: ''; position: absolute; top: 2px; bottom: 2px; width: 8px; background: linear-gradient(to bottom, #1a1a2e, #0d0d14); transform: rotateY(-90deg) translateZ(4px); transform-origin: left center; backface-visibility: hidden; opacity: 0;`. Classe `.card.show-thickness::before { opacity: 1; }`. Versione retro (bordo destro): `.card.show-thickness-back::after` con gradient `#e8d5b8 â†’ #d4b896`, posizionato a destra, con fregio ornamentale ripetuto (SVG pattern ispirazione carta da lettere italiana). Controllo da JS: aggiungere/rimuovere classe quando `Math.abs(currentRotY) > 10`. Testo inciso "laser" sul bordo sinistro fronte giÃ  gestito in markup come `.front-edge-text` (TASK-005/TASK-030). | ✅ | 2026-04-15 |
| TASK-026 | In `js/card.js`: implementare drag & float (I9). Listener `pointerdown` su `.card`: se non su bordo, `isDragging = true; card.style.cursor = 'grabbing'; card.setPointerCapture(e.pointerId);`. Listener `pointermove`: se `isDragging`, calcolare offset da punto di click iniziale, aggiornare `dragOffsetX/Y`. Listener `pointerup`: `isDragging = false;`, avviare spring animation per ritorno al centro: loop rAF con `dragOffsetX = lerp(dragOffsetX, 0, 0.06); dragOffsetY = lerp(dragOffsetY, 0, 0.06);` finchÃ© `Math.abs(dragOffsetX) + Math.abs(dragOffsetY) < 0.5`. In `applyTransform()` aggiungere `translateX(\${dragOffsetX}px) translateY(\${dragOffsetY + floatY}px)`. | ✅ | 2026-04-15 |

### Implementation Phase 4 â€” Front Face "The Engineer" Visual Design & Animations

- GOAL-004: Implementare il design visivo completo della faccia fronte "The Engineer" con materiale carbonio, palette notte, tipografia JetBrains Mono e tutte le micro-animazioni (typing, LED, tracce SVG, logo spin, glitch scan).

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-027 | In `style/front.css`: stilare `.card-front` con `background-color: var(--night-card-bg);`. Aggiungere pattern carbonio woven come `background-image` SVG inline base64: pattern diagonale 45deg, tile 8Ã—8px, colori `var(--night-weave1)` / `var(--night-weave2)`. Overlay microluce: `.card-front::before { content:''; position:absolute; inset:0; background: linear-gradient(135deg, rgba(255,255,255,0.04), transparent); border-radius: var(--radius-front); pointer-events:none; z-index:1; }`. Bordo: `border: 1px solid rgba(0, 140, 255, 0.3);`. Inner glow: `box-shadow: inset 0 0 2px rgba(0,140,255,0.3);`. | ✅ | 2026-04-15 |
| TASK-028 | In `style/front.css`: stilare tutti gli elementi tipografici fronte. `.front-name { font-family: var(--font-mono); font-weight: 700; font-size: 1.1rem; letter-spacing: -0.02em; color: var(--night-text); position: relative; min-height: 1.4em; }`. `.front-role { font-family: var(--font-mono); font-weight: 400; font-size: 0.75rem; color: var(--night-text); opacity: 0.6; margin-top: 4px; }`. `.front-metadata { display: grid; grid-template-columns: 1fr 1fr; gap: 2px 16px; font-family: var(--font-mono); font-weight: 300; font-size: 0.65rem; color: var(--night-meta); text-transform: lowercase; margin-top: auto; }`. Posizionamento globale fronte con padding `20px 24px` e `display: flex; flex-direction: column;`. | ✅ | 2026-04-15 |
| TASK-029 | In `style/front.css`: stilare LED. `.front-leds { position: absolute; top: 16px; right: 16px; display: flex; gap: 8px; }`. `.led { width: 6px; height: 6px; border-radius: 50%; background: var(--night-accent2); border: none; cursor: pointer; padding: 0; }`. `.led.is-off { background: var(--night-meta); opacity: 0.3; }`. Animazione blink CSS: `@keyframes led-pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }`. `.led-1 { animation: led-pulse 2.1s ease-in-out infinite; }`. `.led-2 { animation: led-pulse 3.7s ease-in-out infinite; }`. | ✅ | 2026-04-15 |
| TASK-030 | In `style/front.css`: stilare corner dots (`.front-corner-dots` â€” 3 cerchi 4px in alto a sinistra, `gap: 4px`), bottom row (`.front-bottom` â€” flex, space-between, align-items end), QR container (`.front-qr` â€” `width: 60px; height: 60px;`), logo (`.front-logo` â€” `width: 32px; height: 32px; animation: logo-spin 8s linear infinite;`). Keyframe `@keyframes logo-spin { to { transform: rotateY(360deg); } }`. Edge text: `.front-edge-text { position: absolute; left: -2px; top: 50%; transform: rotate(-90deg) translateX(-50%); font-family: var(--font-mono); font-size: 0.45rem; color: var(--night-accent2); opacity: 0.2; white-space: nowrap; pointer-events: none; }`. | ✅ | 2026-04-15 |
| TASK-031 | In `js/interactions.js`: implementare typing effect per `.front-name`. Funzione `initTyping()`: ottenere testo dal `data-text` attribute. Svuotare `textContent`. Creare `<span class="typing-cursor">|</span>` appeso al nome. Loop con `setTimeout` a 60ms per carattere: ad ogni tick appendere un carattere. Al completamento: cursore lampeggia per 1.5s (via classe CSS `.typing-cursor { animation: blink 0.6s step-end infinite; }`), poi rimuovere cursore. Aggiungere keyframe `@keyframes blink { 50% { opacity: 0; } }` in `style/front.css`. Chiamare `initTyping()` dentro `initInteractions()` e anche su evento `flip:end` se face === 'front'. | ✅ | 2026-04-15 |
| TASK-032 | In `js/interactions.js`: implementare trace SVG reveal. Funzione `initTraces()`: selezionare tutti i `<path>` dentro `.front-traces`. Per ciascun path: calcolare `totalLength = path.getTotalLength()`, settare `stroke-dasharray: totalLength; stroke-dashoffset: totalLength;`. Con delay progressivo (0, 300ms, 600ms, 900ms), animare `stroke-dashoffset` a 0 in 1.2s con `path.style.transition = 'stroke-dashoffset 1.2s ease-out'`. Eseguire una sola volta al caricamento. | ✅ | 2026-04-15 |
| TASK-033 | In `js/interactions.js`: implementare glitch scan (I10). Funzione `initGlitchScheduler()`: `setInterval` con delay random `18000 + Math.random() * 4000` ms (media ~20s). Ad ogni tick: selezionare una `<span>` random dentro `.front-metadata`. Applicare classe `.glitching` per 400ms. In `style/front.css` definire `@keyframes glitch-scan { 0% { transform: skewX(0); filter: none; } 25% { transform: skewX(-5deg); filter: hue-rotate(90deg); } 50% { transform: skewX(3deg) translateX(2px); filter: brightness(2); } 75% { transform: skewX(-2deg); filter: hue-rotate(-60deg); } 100% { transform: skewX(0); filter: none; } }`. `.front-metadata span.glitching { animation: glitch-scan 0.4s steps(4); }`. | ✅ | 2026-04-15 |

### Implementation Phase 5 â€” Back Face "The Human" Visual Design & Animations

- GOAL-005: Implementare il design visivo completo della faccia retro "The Human" con materiale tela lino, palette alba mediterranea, tipografia serif e tutte le micro-animazioni (stagger fade, timeline hover, hobby icons, macchia caffÃ¨, francobollo).

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-034 | In `style/back.css`: stilare `.card-back` con `background-color: var(--dawn-card-bg);`. Texture tela lino via SVG `feTurbulence` inline come `background-image`: `url("data:image/svg+xml,...")` con `<feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4'/>` + `<feDisplacementMap scale='3'/>`. Overlay caldo: `.card-back::before { content:''; position:absolute; inset:0; background: linear-gradient(180deg, rgba(255,255,255,0.3), rgba(255,240,220,0.1)); border-radius: var(--radius-back); pointer-events:none; z-index:1; }`. Bordo: `border: 1px solid rgba(180,120,60,0.25);`. Padding `20px 24px`, `display: flex; flex-direction: column; position: relative; overflow: hidden;`. | ✅ | 2026-04-15 |
| TASK-035 | In `style/back.css`: stilare tipografia retro. `.back-name { font-family: var(--font-display); font-weight: 700; font-style: italic; font-size: 1.15rem; color: var(--dawn-accent); }`. `.back-bio h3 { font-family: var(--font-body); font-weight: 600; font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--dawn-detail); margin-bottom: 4px; }`. `.back-bio p { font-family: var(--font-body); font-weight: 400; font-size: 0.75rem; color: var(--dawn-text); line-height: 1.5; }`. | ✅ | 2026-04-15 |
| TASK-036 | In `style/back.css`: stilare timeline. `.back-timeline { list-style: none; padding: 0; margin: 8px 0; position: relative; padding-left: 16px; border-left: 1px solid var(--dawn-detail); }`. `.back-timeline li { display: flex; align-items: center; gap: 8px; padding: 3px 0; font-family: var(--font-body); font-size: 0.7rem; color: var(--dawn-text); position: relative; cursor: default; }`. `.tl-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--dawn-accent); flex-shrink: 0; position: absolute; left: -19px; }`. `.tl-year { font-weight: 600; color: var(--dawn-detail); min-width: 32px; }`. Tooltip hover: `.back-timeline li::after { content: attr(data-detail); position: absolute; left: 100%; top: 50%; transform: translateY(-50%); background: var(--dawn-text); color: var(--dawn-card-bg); padding: 4px 8px; border-radius: 4px; font-size: 0.6rem; white-space: nowrap; opacity: 0; pointer-events: none; transition: opacity 0.2s; }`. `.back-timeline li:hover::after { opacity: 1; }`. | ✅ | 2026-04-15 |
| TASK-037 | In `style/back.css`: stilare hobby icons. `.back-hobbies { display: flex; gap: 12px; margin: 8px 0; }`. `.hobby { font-size: 1.2rem; background: none; border: none; cursor: pointer; transition: transform 0.2s; padding: 4px; }`. `.hobby:hover { transform: scale(1.3); }`. Stilare bottom row, QR retro, location label. Stilare francobollo `.back-stamp { position: absolute; top: 8px; right: 12px; font-family: var(--font-body); font-weight: 700; font-size: 0.5rem; color: #c0392b; border: 2px solid #c0392b; padding: 2px 6px; transform: rotate(12deg) skewX(-5deg); opacity: 0.7; text-transform: uppercase; letter-spacing: 0.15em; }`. Stilare macchia caffÃ¨ `.back-coffee { position: absolute; bottom: 12px; left: 12px; width: 24px; height: 24px; border-radius: 50%; background: radial-gradient(circle, rgba(139,115,85,0.3), rgba(139,115,85,0.1) 70%, transparent); cursor: pointer; transition: transform 0.3s; }`. `.back-coffee.expanding { transform: scale(1.8); }`. | ✅ | 2026-04-15 |
| TASK-038 | In `js/interactions.js`: implementare stagger fade retro. Listener `document.addEventListener('flip:end', (e) => { if (e.detail.face === 'back') initBackStagger(); })`. Funzione `initBackStagger()`: selezionare tutti i figli diretti di `.card-back` con `querySelectorAll('.card-back > *')`. Inizialmente tutti `opacity: 0; transform: translateY(8px);` (via classe CSS `.back-hidden`). Per ciascun elemento con delay `index * 150`ms: rimuovere `.back-hidden`, aggiungere `.back-reveal` con transition CSS `opacity 0.4s ease-out, transform 0.4s ease-out`. In `style/back.css` aggiungere: `.card-back > .back-hidden { opacity: 0; transform: translateY(8px); }` e `.card-back > .back-reveal { opacity: 1; transform: translateY(0); }`. | ✅ | 2026-04-15 |
| TASK-039 | In `js/interactions.js`: implementare hobby icon micro-animations. Per ciascun `.hobby` button: listener `mouseenter`. Switch su `data-hobby`: `'music'` â†’ creare 3 `<span>` note (`â™ª`) animate con position absolute float-up + fade-out in 1s, poi rimuovere dal DOM. `'travel'` â†’ l'emoji si anima con `@keyframes fly-path { from { transform: translate(0,0) rotate(0deg); } to { transform: translate(20px,-10px) rotate(-15deg); } }` durata 0.6s. `'photo'` â†’ flash bianco full card (`.card-back::after` opacity 1 per 80ms poi 0) + dispatch evento `sound:shutter`. `'nature'` â†’ scale da 1.0 a 1.5 in 0.4s con easing ease-out, poi ritorna (effetto fogliolina che cresce). `'sport'` â†’ micro-bounce (translateY -4px e ritorno) durata 0.3s. | ✅ | 2026-04-15 |

### Implementation Phase 6 â€” Advanced Interactions (Tier 1â€“4) & Custom Cursor

- GOAL-006: Implementare tutte le interazioni Tier 1â€“4 (I1â€“I16) incluse idle showcase, hint flip, scroll zoom, magnetic hover, Konami code, triple click confetti, shake detection, long press, e il sistema cursore personalizzato contestuale.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-040 | In `js/interactions.js` dentro `initInteractions()`: implementare I3 (Scroll Zoom Out). Listener `wheel` su `.scene` (debounce 100ms). `deltaY > 0` â†’ incrementare `zoomLevel` (max 0.6, min 1.0, step 0.02). Applicare `card.style.transform` scale a `cardWrapper`. `deltaY < 0` â†’ decrementare zoomLevel verso 1.0. Easing via `lerp`. Prevenire default scroll della pagina. | ✅ | 2026-04-15 |
| TASK-041 | In `js/interactions.js`: implementare I5 (Idle Showcase). Timer `idleTimer` resettato ad ogni `mousemove`/`click`/`keydown`. Se 15000ms senza interazione: avviare slow rotation su Y (da 0 a 720deg in 12s, mostrando entrambe le facce). `card.style.transition = 'transform 12s linear'; card.style.transform = 'rotateY(720deg)';`. Al primo input utente: cancellare transizione, ripristinare stato. Non triggerare flip events. | ✅ | 2026-04-15 |
| TASK-042 | In `js/interactions.js`: implementare I6 (Hint Flip). Dopo 5000ms senza interazione: mostrare elemento `.flip-hint` (due frecce opposte animate sui bordi lunghi). In `index.html` aggiungere `<div class="flip-hint" aria-hidden="true" hidden><span class="hint-arrow hint-left">â€¹</span><span class="hint-arrow hint-right">â€º</span></div>` dentro `.card`. In `style/interactions.css`: `.flip-hint { position: absolute; inset: 0; pointer-events: none; display: flex; justify-content: space-between; align-items: center; }`. `.hint-arrow { font-size: 1.2rem; color: var(--night-accent); animation: hint-blink 1s ease-in-out infinite; }`. `@keyframes hint-blink { 50% { opacity: 0.2; } }`. Nascondere al primo input. | ✅ | 2026-04-15 |
| TASK-043 | In `js/interactions.js`: implementare I7 (Flip trigger). Listener `dblclick` sulla `.card`. Determinare se il click Ã¨ vicino ai bordi lunghi (sinistra/destra, entro 40px dal bordo): `const rect = card.getBoundingClientRect(); const isEdge = e.clientX - rect.left < 40 || rect.right - e.clientX < 40;`. Se `isEdge`: chiamare `flipCard()` (importata da `card.js`). Altrimenti: ignorare. | ✅ | 2026-04-15 |
| TASK-044 | In `js/interactions.js`: implementare I8 (Magnetic Hover bordo corto). Listener `mousemove` su `.card`: calcolare se cursore Ã¨ entro 30px dal bordo corto (top/bottom). Se sÃ¬: aggiungere tilt extra Â±5deg verso il cursore (modificando `targetTiltX` via funzione esportata `setTiltBias(x, y)` da `card.js`). Se no: `setTiltBias(0, 0)`. | ✅ | 2026-04-15 |
| TASK-045 | In `js/interactions.js`: implementare I13 (Konami Code). Mantenere array `konamiBuffer = []`. Sequenza target: `['ArrowUp','ArrowUp','ArrowDown','ArrowDown','ArrowLeft','ArrowRight','ArrowLeft','ArrowRight','KeyB','KeyA']`. Listener `keydown`: push `e.code` in buffer, mantenere ultimi 10 elementi. Se `buffer.join(',') === target.join(',')`: aggiungere classe `.pixel-mode` a `.card` per 3000ms. In `style/interactions.css`: `.card.pixel-mode { image-rendering: pixelated; filter: contrast(1.5) saturate(0.5); }`. `.card.pixel-mode .card-front, .card.pixel-mode .card-back { font-family: 'Courier New', monospace !important; image-rendering: pixelated; }`. Dispatch evento `sound:konami`. | ✅ | 2026-04-15 |
| TASK-046 | In `js/interactions.js`: implementare I14 (Triple Click Confetti). Tracciare click su `.front-name, .back-name` con timestamp. Se 3 click entro 500ms: generare 30 `<div class="confetti">` con posizione random, colori random tra `[#2979ff, #00e5ff, #c4622d, #5a7a52, #d4862a]`, animazione `@keyframes confetti-fall { from { transform: translateY(0) rotate(0); opacity:1; } to { transform: translateY(200px) rotate(720deg); opacity:0; } }` durata 1.5s. Rimuovere dal DOM al termine. Dispatch `sound:confetti`. | ✅ | 2026-04-15 |
| TASK-047 | In `js/interactions.js`: implementare I15 (Shake / mouse velocity alta). Tracciare velocitÃ  mouse: calcolare distanza tra ultime 2 posizioni / deltaTime. Se velocitÃ  > 2000px/s per 3 frame consecutivi: aggiungere `.shaking` a `.card` per 600ms. In `style/interactions.css`: `.card.shaking { animation: shake-glitch 0.6s; }`. `@keyframes shake-glitch { 0%,100% { transform: translate(0); filter: none; } 20% { transform: translate(-3px,2px) skewX(2deg); filter: hue-rotate(90deg); } 40% { transform: translate(2px,-1px) skewX(-1deg); filter: brightness(1.5); } 60% { transform: translate(-1px,3px); filter: hue-rotate(-45deg); } 80% { transform: translate(3px,-2px) skewX(1deg); filter: contrast(1.3); } }`. Dispatch `sound:glitch`. | ✅ | 2026-04-15 |
| TASK-048 | In `js/interactions.js`: implementare I16 (Long Press piega angoli). Listener `pointerdown` su `.card`: avviare timer 1500ms. Se `pointerup` prima â†’ cancellare timer. Se timer scatta: aggiungere classe `.corner-fold` a `.card` per 2000ms. In `style/interactions.css`: `.card.corner-fold .card-front::after, .card.corner-fold .card-back::after` (override specular) oppure dedicare 4 pseudo-elementi angolari via wrapper. Implementare come: `.card.corner-fold { --fold: 1; }` con 4 `<div class="fold-corner">` (uno per angolo) posizionati negli angoli, `clip-path: polygon(0 0, 100% 0, 0 100%)`, background leggermente piÃ¹ scuro, `transform: rotate(Xdeg)`, transizione `transform 0.3s`. | ✅ | 2026-04-15 |
| TASK-049 | In `style/interactions.css`: implementare cursori personalizzati. `.scene { cursor: crosshair; }` â€” bianco per notte. `.scene.is-dawn { cursor: url("data:image/svg+xml,...nero crosshair...") 12 12, crosshair; }` â€” nero per alba. `.card { cursor: url("data:image/svg+xml,...cerchio con punto centrale...") 12 12, pointer; }` â€” come da design plan 7.5. `.card:active, .card.dragging { cursor: grabbing; }`. `.card .led { cursor: pointer; }` â€” con alone (SVG custom). Zone bordo lungo della card: `cursor: ew-resize;` per hint flip (due frecce opposte orizzontali). | ✅ | 2026-04-15 |

### Implementation Phase 7 â€” Audio System (Opt-in, Synthesis, Event Bus)

- GOAL-007: Implementare il sistema audio completo: toast opt-in con persistenza localStorage, engine di synthesis Web Audio API per tutti i 12 eventi sonori (S1â€“S12), event bus per accoppiamento lasco con interazioni, e lazy-loading opzionale di Tone.js.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-050 | In `js/audio.js` funzione `initAudio()`: controllare `localStorage.getItem('soundEnabled')`. Se `null` (prima visita): listener one-shot su primo `click` del document â†’ mostrare `.audio-toast` (rimuovere `hidden`). Bottone Yes: `localStorage.setItem('soundEnabled', 'true'); enableAudio(); hideToast();`. Bottone No: `localStorage.setItem('soundEnabled', 'false'); hideToast();`. Se `'true'`: chiamare `enableAudio()` direttamente. Se `'false'`: non fare nulla. | ✅ | 2026-04-15 |
| TASK-051 | In `style/interactions.css`: stilare `.audio-toast { position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%); background: rgba(20,20,30,0.95); color: #e8eaf0; padding: 12px 20px; border-radius: 8px; font-family: var(--font-mono); font-size: 0.8rem; display: flex; align-items: center; gap: 12px; z-index: var(--z-ui); backdrop-filter: blur(8px); border: 1px solid rgba(255,255,255,0.1); }`. `.audio-toast button { background: var(--night-accent); color: white; border: none; padding: 6px 14px; border-radius: 4px; cursor: pointer; font-family: var(--font-mono); font-size: 0.75rem; }`. `.audio-toast button.no { background: transparent; border: 1px solid rgba(255,255,255,0.2); }`. | ✅ | 2026-04-15 |
| TASK-052 | In `js/audio.js`: funzione `enableAudio()`. Creare `AudioContext` (`new (window.AudioContext || window.webkitAudioContext)()`). Creare `masterGain` con `gain.value = 0.35`. Connettere `masterGain` a `ctx.destination`. Definire oggetto `sounds = {}` con factory function per ciascun suono. Implementare funzioni synthesis per S1â€“S12 usando Web Audio API nativa: **S1** (`playTick`): OscillatorNode triangle 800Hz, durata 80ms, gain envelope decay 0.08s. **S2** (`playFlipStart`): noise buffer (white noise) filtrato con BiquadFilter bandpass 400Hz Q=2 + OscillatorNode sine sweep 200â†’800Hz in 0.5s. **S3** (`playFlipLand`): OscillatorNode sine 80Hz con pitch envelope decay 0.3s (membrane-like). **S4** (`playLedClick`): OscillatorNode square 2000Hz per 30ms + noise burst 20ms. | ✅ | 2026-04-15 |
| TASK-053 | In `js/audio.js`: continuare synthesis. **S5** (`playMusicNote`): OscillatorNode sine 523Hz (C5), gain ADSR attack 0.01 release 0.5. **S6** (`playSwoosh`): noise filtrato con BiquadFilter highpass sweep 200â†’4000Hz in 0.3s. **S7** (`playShutter`): noise burst 30ms con gain instantaneo. **S8** (`playCoffee`): noise filtrato lowpass 300Hz, gain oscillante 4Hz per 600ms (gorgoglio). **S9** (`playKonami`): 3 note sequenziali square wave [523, 659, 784] Hz, 100ms ciascuna, gap 50ms. **S10** (`playConfetti`): OscillatorNode sine sweep 400â†’1200Hz in 0.15s. **S11**: nessun suono. **S12** (`playGlitch`): noise con WaveShaperNode distortion per 100ms. | ✅ | 2026-04-15 |
| TASK-054 | In `js/audio.js`: implementare event bus audio. Listener su `document` per custom events: `document.addEventListener('sound:tick', () => sounds.S1?.())`, `document.addEventListener('sound:flipStart', () => sounds.S2?.())`, `document.addEventListener('sound:flipLand', () => sounds.S3?.())`, `document.addEventListener('sound:ledClick', () => sounds.S4?.())`, ecc. per tutti S1â€“S12. In `js/interactions.js` e `js/card.js`: dispatch degli eventi `sound:*` nei punti appropriati (giÃ  indicati nei task precedenti). | ✅ | 2026-04-15 |
| TASK-055 | In `js/audio.js`: implementare toggle tastiera. Listener `keydown` per tasto `KeyA`: toggle `soundEnabled`. Se disabilitato: `masterGain.gain.value = 0; localStorage.setItem('soundEnabled', 'false')`. Se riabilitato: `masterGain.gain.value = 0.35; localStorage.setItem('soundEnabled', 'true')`. Implementare transizione sonora al flip (design plan 6.3): su `flip:start` fade-out toni correnti in 80ms, trigger S2; su `flip:midpoint` crossfade ambianza; su `flip:end` trigger S3 + fade-in nuova ambianza; durata totale 2.2s. Implementare opzionale lazy-load Tone.js: se `enableAudio()` non riesce a creare AudioContext (fallback): inserire `<script src="https://cdn.jsdelivr.net/npm/tone@latest/build/Tone.min.js">` con integrity hash, poi usare `Tone.Synth` al posto delle factory native. | ✅ | 2026-04-15 |

### Implementation Phase 8 â€” Responsive, Accessibility, Performance Polish & Documentation

- GOAL-008: Finalizzare responsive design su 3 breakpoint con behavior adattivo (swipe mobile, gyroscope, riduzione effetti), implementare pieno supporto accessibilitÃ  (`prefers-reduced-motion`, keyboard nav, focus management), ottimizzare performance (compositing, debounce, object pooling), e aggiornare documentazione.

| Task | Description | Completed | Date |
| -------- | --------------------- | --------- | ---- |
| TASK-056 | In `style/card.css` e `style/scene.css`: aggiungere media queries. `@media (max-width: 900px)`: `.card { width: 360px; height: 227px; }`. `@media (max-width: 600px)`: `.card { width: 300px; height: 189px; }`. `.card-wrapper { perspective: 600px; }`. In `js/card.js`: detectare breakpoint con `window.matchMedia('(max-width: 900px)')`. Se tablet: clampare tilt a Â±10deg. Se mobile: disabilitare drag (I9), dimezzare particelle via `scene.setParticleMultiplier(0.5)`. | ✅ | 2026-04-15 |
| TASK-057 | In `js/card.js`: implementare swipe flip mobile. Su dispositivi touch (detect via `'ontouchstart' in window`): listener `touchstart` / `touchmove` / `touchend` su `.card`. Calcolare swipe orizzontale: se `deltaX > 80px` e `deltaTime < 300ms` â†’ chiamare `flipCard()`. Ignorare swipe verticali. | ✅ | 2026-04-15 |
| TASK-058 | In `js/card.js`: implementare tilt gyroscope mobile. Se `window.DeviceOrientationEvent` disponibile: richiedere permesso (iOS 13+: `DeviceOrientationEvent.requestPermission()`). Listener `deviceorientation`: mappare `event.gamma` (Â±90) a tilt Y (Â±15deg) e `event.beta` (Â±90) a tilt X (Â±12deg). Smoothing lerp 0.05. Fallback: se permesso negato o API non disponibile, nessun tilt su mobile. | ✅ | 2026-04-15 |
| TASK-059 | In `style/base.css`: aggiungere `@media (prefers-reduced-motion: reduce)`. Dentro: `.card { animation: none !important; transition: none !important; }`. `.led { animation: none !important; }`. `.world-particles, .world-grain { display: none; }`. `.front-logo { animation: none !important; }`. `.card::after { display: none; }` (rimuovere specular). Tutte le classi animate (`.glitching`, `.shaking`, `.pixel-mode`, ecc.): `animation: none !important;`. In `js/card.js`: se `window.matchMedia('(prefers-reduced-motion: reduce)').matches` â†’ disabilitare float, tilt permane statico, flip istantaneo (duration 0), niente idle showcase. | ✅ | 2026-04-15 |
| TASK-060 | In `style/base.css`: aggiungere `@media (prefers-reduced-transparency: reduce)`. Dentro: `.world-grain { display: none; }`. `.world-vignette { display: none; }`. `.card-front::before, .card-back::before { display: none; }` (overlay trasparenti). | ✅ | 2026-04-15 |
| TASK-061 | In `js/interactions.js` e `index.html`: implementare navigazione tastiera completa. La `.card` ha `tabindex="0"`. Listener `keydown` su `.card`: `Enter` o `Space` â†’ `flipCard()`. `Escape` â†’ nascondere hint flip. Aggiungere `role="button"` e `aria-pressed` ai LED. Aggiungere `aria-label` descrittive a tutti i bottoni interattivi. Focus ring visibile: in `style/base.css` aggiungere `.card:focus-visible { outline: 2px solid var(--night-accent); outline-offset: 4px; }`. `.scene.is-dawn .card:focus-visible { outline-color: var(--dawn-accent); }`. | ✅ | 2026-04-15 |
| TASK-062 | In `js/card.js`: ottimizzazioni performance. Assicurare che tutte le trasformazioni usino solo `transform` e `opacity` (proprietÃ  composited GPU). In `js/scene.js`: implementare object pooling per particelle â€” pre-allocare array di 180 oggetti, riutilizzare invece di creare/distruggere. Debounce `mousemove` a 16ms (match rAF rate). Throttle `wheel` a 100ms. Debounce `resize` a 250ms. In `js/interactions.js`: rate-limit easter eggs (cooldown 3s per Konami, 2s per confetti, 1s per shake). | ✅ | 2026-04-15 |
| TASK-063 | Creare file SVG placeholder in `assets/`: `assets/qr-github.svg` (QR placeholder 60Ã—60, pixel arrotondati, tinta blu `#2979ff`), `assets/qr-personal.svg` (QR placeholder 60Ã—60, pixel arrotondati, tinta terracotta `#c4622d`), `assets/logo.svg` (placeholder simbolo geometrico 32Ã—32, colore `#2979ff`). Tutti i file devono essere SVG validi con `viewBox`, senza dipendenze esterne. | ✅ | 2026-04-15 |
| TASK-064 | In `js/interactions.js`: implementare I4 (Scroll Card interno). Se il cursore Ã¨ sopra la `.card` e il contenuto retro (`.card-back`) overflow: applicare `overflow-y: auto` a `.card-back` e intercettare `wheel` sulla card per scrollare il contenuto interno senza propagare alla pagina (`e.stopPropagation()`). Aggiungere in `style/back.css`: `.card-back { overflow-y: auto; scrollbar-width: thin; scrollbar-color: var(--dawn-detail) transparent; }`. `::-webkit-scrollbar { width: 3px; }`. | ✅ | 2026-04-15 |
| TASK-065 | In `js/interactions.js`: implementare I11 (Coffee Click). Listener `click` su `.back-coffee`. Al click: aggiungere classe `.expanding` (scale 1.8 in 0.3s via CSS transition giÃ  definita in TASK-037). `setTimeout(1000, () => rimuovere .expanding)`. Dispatch `sound:coffee`. In `js/interactions.js`: implementare I12 (LED Click). Listener `click` su `.led-1, .led-2`. Toggle classe `.is-off`. Se `.is-off`: fermare animazione CSS (`animation: none`). Se rimossa: ripristinare animazione. Dispatch `sound:ledClick`. | ✅ | 2026-04-15 |
| TASK-066 | Aggiornare `README.md` con: sezione "Duality Card" con descrizione progetto, screenshot placeholder, lista feature (dual-face 3D, living world, 16 interactions, audio synthesis), requisiti browser, istruzioni sviluppo locale (`npx serve .` o apertura diretta `index.html`), configurazione QR (istruzioni per sostituire SVG placeholder con QR reali), toggle audio (tasto `A`), limitazioni note (no WebGL, OffscreenCanvas opzionale), licenza. | ✅ | 2026-04-15 |
| TASK-067 | In `index.html`: aggiungere landscape hint per mobile. `<div class="landscape-hint" aria-hidden="true" hidden>ðŸ“± Ruota il dispositivo per un'esperienza migliore</div>`. In `style/interactions.css`: `.landscape-hint { position: fixed; bottom: 16px; left: 50%; transform: translateX(-50%); background: rgba(0,0,0,0.8); color: white; padding: 8px 16px; border-radius: 8px; font-size: 0.75rem; z-index: var(--z-ui); }`. In `js/main.js`: mostrare hint solo su mobile portrait (`window.innerWidth < 600 && window.innerHeight > window.innerWidth`) per 4s poi nascondere. | ✅ | 2026-04-15 |

## 3. Alternatives

- **ALT-001**: **WebGL/Three.js per rendering 3D completo** â€” Non scelto perchÃ© il design plan specifica esplicitamente CSS transforms + perspective per massima compatibilitÃ  e performance. WebGL aggiungerebbe complessitÃ , bundle size significativo e problemi di compatibilitÃ  mobile senza benefici proporzionali per un singolo oggetto 3D.
- **ALT-002**: **Framework SPA (React/Vue/Svelte)** â€” Non scelto perchÃ© il design plan richiede stack vanilla per massima performance e zero overhead. Un framework aggiungerebbe almeno 30â€“50KB di runtime per un'esperienza single-page senza routing o state management complesso.
- **ALT-003**: **GSAP per tutte le animazioni** â€” Non scelto come dipendenza primaria per rispettare il budget JS <80KB e mantenere controllo diretto su rAF/CSS. GSAP potrebbe essere considerato come dipendenza opzionale solo per spring physics del drag se l'implementazione CSS-only risultasse inadeguata.
- **ALT-004**: **File audio preregistrati** â€” Non scelto primariamente per il vincolo di leggerezza (12 file Ã— 50KB = 600KB aggiuntivi). La synthesis Web Audio produce suoni equivalenti a costo zero in termini di network. Tone.js mantenuto come fallback lazy-loaded.
- **ALT-005**: **CSS-only flip senza JavaScript** â€” Non scelto perchÃ© la sincronizzazione flipâ†”sfondoâ†”particelleâ†”audio richiede orchestrazione programmatica. CSS-only non permetterebbe il dispatch di eventi lifecycle intermedi (`flip:midpoint`).
- **ALT-006**: **SVG `<feTurbulence>` per grain invece di Canvas** â€” Considerato ma non scelto come primario per via delle performance: `feTurbulence` animato causa repaint costosi. Canvas offscreen con pixel manipulation Ã¨ piÃ¹ performante e controllabile.

## 4. Dependencies

- **DEP-001**: **Google Fonts CDN** â€” JetBrains Mono (wght 300;400;700), Playfair Display (ital wght 0,700;1,400;1,700), Lora (wght 400;600;700). Caricati con `font-display: swap` e fallback stack locale. Non bloccanti per il rendering.
- **DEP-002**: **Web API native** â€” `requestAnimationFrame`, `performance.now()`, `CanvasRenderingContext2D`, `OffscreenCanvas` (opzionale), `PointerEvent`, `TouchEvent`, `DeviceOrientationEvent` (mobile opzionale), `AudioContext` / `webkitAudioContext`, `matchMedia`, `localStorage`, `CustomEvent`, `ResizeObserver` (opzionale per card resize detection).
- **DEP-003**: **Tone.js** (opzionale, lazy-loaded) â€” Caricato da CDN `cdn.jsdelivr.net/npm/tone@latest/build/Tone.min.js` con attributo `integrity` solo se l'utente abilita l'audio E il Web Audio API nativo fallisce. Non caricato al page load. ~150KB non gzipped.
- **DEP-004**: **Asset SVG locali** â€” `assets/qr-github.svg`, `assets/qr-personal.svg`, `assets/logo.svg`. Creati come placeholder durante l'implementazione, sostituibili dall'utente con SVG reali.
- **DEP-005**: **GitHub Pages** â€” Hosting statico. Richiede file `.nojekyll` nella root per servire directory con prefisso underscore (non usate attualmente, ma best practice). Nessun build step richiesto.

## 5. Files

- **FILE-001**: `index.html` â€” Markup semantico completo della scena 3D. Contiene: meta tags SEO/OG, link ai 6 CSS, struttura `.scene` con 4 world layers + `.card-wrapper` > `.card` > `.card-front` / `.card-back`, contenuti fronte (dots, LEDs, name, role, metadata, QR, logo, edge text) e retro (stamp, header, bio, timeline, hobbies, QR, location, coffee stain), toast audio, hint flip, landscape hint, script module entry point.
- **FILE-002**: `style/base.css` â€” CSS reset, tutte le custom properties (palette notte/alba, timing, z-index, breakpoints, font stacks, dimensioni card), importazione Google Fonts, stili body/html, media queries `prefers-reduced-motion` e `prefers-reduced-transparency`.
- **FILE-003**: `style/scene.css` â€” Stili scena full-viewport, layer gradient (notte/alba), canvas particelle, vignetta radiale, canvas grain, transizioni stato `.is-dawn`, tint aureola.
- **FILE-004**: `style/card.css` â€” Perspective container, card 3D `preserve-3d`, face `backface-visibility:hidden`, dimensioni responsive, specular highlight `::after`, thickness `::before`, shadow dynamica via custom properties, media queries breakpoint per dimensioni card.
- **FILE-005**: `style/front.css` â€” Materiale carbonio woven (SVG tile), overlay microluce, bordo blu + inner glow, tipografia JetBrains Mono (name, role, metadata), LED stili + blink keyframes, corner dots, QR container, logo spin keyframe, edge text, glitch-scan keyframe, typing cursor blink keyframe.
- **FILE-006**: `style/back.css` â€” Materiale tela lino (SVG turbulence), overlay caldo, bordo oro antico, tipografia Playfair/Lora, layout bio, timeline con tooltip hover, hobby icons, francobollo TORINO, macchia caffÃ¨, stagger fade classes (`.back-hidden`, `.back-reveal`), QR retro, scrollbar styling.
- **FILE-007**: `style/interactions.css` â€” Cursori personalizzati (crosshair, grab, ew-resize, pointer), audio toast stili, flip hint arrows + blink keyframe, Konami pixel-mode, shake-glitch keyframe, confetti-fall keyframe, corner-fold stili, landscape hint, focus-visible ring.
- **FILE-008**: `js/main.js` â€” Entry point ES module. Importa e chiama in sequenza `initScene()`, `initCard()`, `initInteractions()`, `initAudio()` su `DOMContentLoaded`. Gestisce landscape hint mobile.
- **FILE-009**: `js/scene.js` â€” State machine `night`/`dawn`, sistema particelle (classe Particle, createStars/createPollen, render loop, parallasse, transitionParticles con crossfade 800ms), grain noise canvas (renderGrain con ImageData, OffscreenCanvas opzionale), monitor fps (rolling average, adattamento automatico), export `initScene`, `updateScene`, `getWorldState`, `setParticleMultiplier`, `isLowPerformance`.
- **FILE-010**: `js/card.js` â€” Loop rAF principale con delta-time, float sinusoidale (amp 10px, period 3.2s, micro-rotazioni idle), tilt mouse con lerp 0.08 e clamp, apply transform (translate + rotateXYZ + flip offset), ombra dinamica calcolata, specular highlight posizionamento, flip con lifecycle events (start/midpoint/end), drag con spring return, tilt bias API per magnetic hover, swipe touch mobile, gyroscope tilt, responsive breakpoint detection. Export `initCard`, `flipCard`, `isCardFlipped`, `setTiltBias`.
- **FILE-011**: `js/interactions.js` â€” Orchestratore interazioni I1â€“I16. Typing effect, SVG trace reveal, glitch scheduler, back stagger fade, hobby icon micro-animations, scroll zoom (I3), scroll card (I4), idle showcase (I5), hint flip (I6), flip trigger dblclick edge (I7), magnetic hover (I8), Konami code (I13), triple click confetti (I14), shake detect (I15), long press fold (I16), coffee click (I11), LED click (I12). Rate-limiting e cooldown per easter eggs. Export `initInteractions`.
- **FILE-012**: `js/audio.js` â€” Sistema audio opt-in. Toast gestione, localStorage persistenza. AudioContext creation, masterGain 0.35. 12 synthesis functions (S1â€“S12) usando Web Audio API nativa (OscillatorNode, GainNode, BiquadFilterNode, WaveShaperNode, noise buffer). Event bus listener per `sound:*` custom events. Toggle tastiera `A`. Lazy-load Tone.js fallback. Export `initAudio`.
- **FILE-013**: `assets/qr-github.svg` â€” QR code SVG per GitHub profile. Pixel arrotondati, tinta blu `#2979ff`, viewBox 60Ã—60.
- **FILE-014**: `assets/qr-personal.svg` â€” QR code SVG per messaggio personale. Pixel arrotondati, tinta terracotta `#c4622d`, viewBox 60Ã—60.
- **FILE-015**: `assets/logo.svg` â€” Simbolo/logo personale geometrico. Usato fronte (rotazione 3D) e retro (sigillo ink stamp). Colore `#2979ff`, viewBox 32Ã—32.
- **FILE-016**: `.nojekyll` â€” File vuoto per GitHub Pages.
- **FILE-017**: `README.md` â€” Documentazione aggiornata con descrizione Duality Card, feature list, requisiti browser, istruzioni sviluppo, configurazione QR/audio, limitazioni note.

## 6. Testing

- **TEST-001**: **Flip lifecycle** â€” Verificare che doppio click su bordo lungo card (entro 40px) triggeri flip. Verificare dispatch sequenziale di `flip:start` â†’ `flip:midpoint` (a 1000ms) â†’ `flip:end` (a 2000ms). Verificare che `flip:midpoint` scateni switch classe `.is-dawn` su `.scene`. Verificare che flip ritorni correttamente (albaâ†’notte). Verificare che click fuori bordo non triggeri flip.
- **TEST-002**: **Particelle crossfade** â€” Verificare che al `flip:midpoint` le particelle attive (stelle) inizino fade-out (opacity â†’ 0 in 800ms). Verificare che contemporaneamente nuove particelle (pollini) inizino fade-in (opacity 0 â†’ target in 800ms). Verificare parametri: stelle 180 count / 0.5â€“2px / drift 0.015, pollini 60 count / ovale / drift 0.03.
- **TEST-003**: **Tilt e specular** â€” Verificare che mousemove aggiorni inclinazione card con lerp. Verificare clamp Â±18deg X / Â±22deg Y su desktop. Verificare clamp Â±10deg su tablet (600â€“900px). Verificare che specular highlight (pseudo-elemento ::after) si muova in correlazione inversa al tilt. Verificare opacity max 0.15.
- **TEST-004**: **Performance adaptation** â€” Simulare calo fps (throttle CPU in DevTools): verificare che dopo 2s sotto 30fps il count particelle si dimezzi. Verificare che grain interval passi da 80ms a 160ms. Verificare che se fps risale >45 per 2s, i valori si ripristinino.
- **TEST-005**: **Interazioni I1â€“I16 matrice** â€” Per ciascuna interazione, verificare: trigger corretto, effetto visivo atteso, durata effetto, cleanup post-effetto, evento audio dispatch (se applicabile). Verificare rate-limiting: Konami cooldown 3s, confetti cooldown 2s, shake cooldown 1s.
- **TEST-006**: **Audio opt-in** â€” Verificare che al primo click su pagina compaia toast audio. Verificare che "Yes" attivi AudioContext e salvi `localStorage.soundEnabled = 'true'`. Verificare che "No" salvi `'false'` e non crei AudioContext. Verificare che a reload successivo il toast non riappaia. Verificare che tasto `A` toggli audio. Verificare master volume a 0.35.
- **TEST-007**: **prefers-reduced-motion** â€” Attivare `prefers-reduced-motion: reduce` in DevTools. Verificare: nessun float, nessun tilt animato, nessun LED blink, nessun grain/particelle visibili, flip istantaneo (0ms), nessun idle showcase, nessun glitch scan, nessun shake animation. Contenuto statico leggibile.
- **TEST-008**: **Keyboard navigation** â€” Verificare: `Tab` porta focus su `.card` (focus ring visibile). `Enter`/`Space` su card triggera flip. `Tab` naviga a LED (focusable button). `Enter` su LED togla on/off. `Escape` chiude hint flip. `A` toggle audio. Konami code funziona da tastiera.
- **TEST-009**: **Responsive breakpoints** â€” Desktop >900px: card 420Ã—265, tutti effetti. Tablet 600â€“900px: card 360Ã—227, tilt Â±10deg. Mobile <600px: card 300Ã—189, swipe flip, no drag, particelle dimezzate, landscape hint in portrait.
- **TEST-010**: **Cross-browser compatibility** â€” Testare su: Chrome 90+ (desktop + Android), Firefox 90+ (desktop), Safari 15+ (desktop + iOS), Edge 90+. Verificare: rendering `backface-visibility`, `perspective`, CSS `transform-style: preserve-3d`, `OffscreenCanvas` fallback, `AudioContext` creation, touch events, `DeviceOrientationEvent.requestPermission()` su iOS.

## 7. Risks & Assumptions

- **RISK-001**: **Sovraccarico GPU su mobile** â€” Combinazione di particelle canvas + grain canvas + CSS 3D transforms + dynamic shadows puÃ² causare frame drop su dispositivi mobile mid-range. Mitigazione: performance monitor adattivo (PER-003), dimezzamento particelle su mobile, disabilitazione grain su low-perf.
- **RISK-002**: **Inconsistenze `backface-visibility` su Safari/iOS** â€” Safari ha storicamente bug con `backface-visibility: hidden` in contesti `preserve-3d` annidati. Mitigazione: testing early su Safari, eventuali workaround con `transform: translateZ(0.1px)` per forzare layer separation.
- **RISK-003**: **Conflitto gesture touch** â€” Swipe flip orizzontale puÃ² confliggere con scroll interno card (I4) o scroll pagina. Mitigazione: implementare gesture disambiguation con threshold distanza + angolo (orizzontale >60Â° = flip, verticale = scroll).
- **RISK-004**: **AudioContext bloccato da browser policy** â€” Chrome e Safari richiedono user gesture per creare/riprendere AudioContext. Mitigazione: AudioContext creato solo dopo primo click confermato (toast "Yes"), con `ctx.resume()` esplicito.
- **RISK-005**: **Eccesso interazioni riduce discoverability** â€” 16 interazioni + 12 suoni possono confondere l'utente. Mitigazione: tier system con discovery progressiva, hint subtili, nessun effetto blocca UX principale.
- **RISK-006**: **Google Fonts latenza/blocco** â€” CDN Google Fonts puÃ² introdurre FOUT/FOIT. Mitigazione: `font-display: swap` + fallback stack completo per ciascun font.
- **RISK-007**: **OffscreenCanvas non supportato** â€” Firefox ha supporto limitato per `OffscreenCanvas`. Mitigazione: detection esplicita con fallback a canvas onscreen (giÃ  previsto in TASK-014).
- **ASSUMPTION-001**: Il progetto resta un sito statico servito da GitHub Pages senza build pipeline. Tutti i file sono direttamente serviti dalla root.
- **ASSUMPTION-002**: I testi biografici (timeline, bio) e gli asset SVG finali (QR reali, logo definitivo) sono disponibili o sostituibili senza impatto strutturale. Il piano usa placeholder.
- **ASSUMPTION-003**: L'utente accetta degradazione graceful su dispositivi low-end: meno particelle, niente grain, niente drag, flip semplificato â€” ma contenuto sempre leggibile.
- **ASSUMPTION-004**: Tone.js Ã¨ accettabile come dipendenza opzionale lazy-loaded. Non viene caricato se l'utente non abilita l'audio o se il Web Audio API nativo funziona correttamente.
- **ASSUMPTION-005**: Il file `index.html` attuale (1876 righe, design precedente) viene completamente sostituito. Nessun contenuto del design precedente viene preservato ad eccezione dei meta tags SEO/OG.

## 8. Related Specifications / Further Reading

- [Design Plan sorgente](../duality-card-design-plan.md)
- [CSS 3D Transforms â€” MDN](https://developer.mozilla.org/docs/Web/CSS/transform-function/rotate3d)
- [CSS `perspective` â€” MDN](https://developer.mozilla.org/docs/Web/CSS/perspective)
- [Canvas API â€” MDN](https://developer.mozilla.org/docs/Web/API/Canvas_API)
- [OffscreenCanvas â€” MDN](https://developer.mozilla.org/docs/Web/API/OffscreenCanvas)
- [Web Audio API â€” MDN](https://developer.mozilla.org/docs/Web/API/Web_Audio_API)
- [Tone.js Documentation](https://tonejs.github.io/)
- [prefers-reduced-motion â€” MDN](https://developer.mozilla.org/docs/Web/CSS/@media/prefers-reduced-motion)
- [WCAG 2.1 Quick Reference](https://www.w3.org/WAI/WCAG21/quickref/)
- [Pointer Events API â€” MDN](https://developer.mozilla.org/docs/Web/API/Pointer_events)
- [DeviceOrientationEvent â€” MDN](https://developer.mozilla.org/docs/Web/API/DeviceOrientationEvent)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)





